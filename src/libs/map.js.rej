diff a/src/libs/map.js b/src/libs/map.js	(rejected hunks)
@@ -72,8 +72,8 @@ const companiesAlgoliaResultsToMapMarkers = (searchResults = []) => {
 			}
 		});
 	});
-	return companyMarkers;
 };
+const companiesResultsToMapMarkers = companiesAlgoliaResultsToMapMarkers;
 
 const companiesFileToMapMarkers = ({ edges }) => {
 	let markers = [];
